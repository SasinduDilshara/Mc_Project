#ifndef UTILS_UNIQUE
#define UTILS_HUNIQUE

double mean(double arr[], int len);
double sd(double arr[], int len, double mean);
capXMLchar *capxml,double t_mean, double h_mean, double p_mean, double l_mean,double t_sd, double h_sd, double p_sd, double l_sd,String identifier, String datetime);

#endif